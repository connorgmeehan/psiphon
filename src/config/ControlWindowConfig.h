#pragma once

#include "ofColor.h"

struct{
    ofColor borderColour = ofColor(65, 65, 75);
    ofColor borderTextColour = ofColor(240, 240, 240);
    ofColor windowColour = ofColor(45, 45, 55);
    ofColor tabColour = ofColor  (200, 55, 55);
    int topBorderHeight = int(12);
} controlWindowConfig;

